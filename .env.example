# Copy to .env and fill in. install.sh reads these to fill secret placeholders.
# (.env is gitignored; never commit real keys.)

# context7 MCP API key (https://context7.com)
CONTEXT7_API_KEY=

# Exa web-search API key (https://exa.ai)
EXA_API_KEY=
